<%= form_with(model: admin_vehicle_type) do |form| %>
  <% if admin_vehicle_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_vehicle_type.errors.count, "error") %> prohibited this admin_vehicle_type from being saved:</h2>

      <ul>
        <% admin_vehicle_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :required_space, style: "display: block" %>
    <%= form.number_field :required_space %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
